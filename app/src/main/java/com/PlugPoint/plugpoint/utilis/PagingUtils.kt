package com.PlugPoint.plugpoint.utilis

/**
 * Global paging constants for Firestore / REST pagination.
 */
object PagingUtils {
    /** Default number of items to fetch per page when infinite-scrolling. */
    const val DEFAULT_PAGE_SIZE = 20
}
